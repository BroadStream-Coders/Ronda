import { History } from "lucide-react";

import type { GameMeta } from "@/game/kit";

export const meta: GameMeta = {
  id: "cronos",
  name: "Cronos",
  description: "Ordenar hechos en la línea de tiempo",
  icon: History,
};
